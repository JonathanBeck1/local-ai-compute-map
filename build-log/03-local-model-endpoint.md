# Phase 03 — Local model endpoint

**Status:** not started **Date:** **Elapsed:** **Cost:**

## Goal

Stand up the one local model endpoint for the fleet on the CUDA box and point a coding agent at it. Also day zero of the workload log: from here on, every job's peak memory, tokens per second, and capacity misses get recorded, because hardware decisions should follow measurements rather than the other way round.

## Hardware touched

Linux workstation; development console as client

## Planned steps

- Install the runtime and serve a model sized to fit 12 GB.
- Confirm the endpoint answers from another machine on the mesh.
- Point the coding agent at the local endpoint and confirm a round trip.
- Start the workload log.

## What I ran

_Not started._

## Output

_Pasted verbatim when the phase runs._

## What broke

_An empty section here after the phase is done would be suspicious, not impressive._

## What I would do differently

_Filled in after._

## Acceptance check

_The phase is done when this section holds pasted output, not when the commands have been typed._
