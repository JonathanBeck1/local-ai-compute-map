# Phase 02 — Verify the Linux box, driver, and containers

**Status:** not started
**Date:**
**Elapsed:**
**Cost:**

## Goal

Establish that the CUDA machine is in a known-good state before anything is asked of it: distribution, kernel, driver branch, container runtime, and GPU visibility from inside a container.

## Hardware touched

Linux workstation (RTX 4070, 12 GB VRAM, 64 GB RAM)

## Planned steps

- Record distribution, kernel, and driver versions as facts rather than assumptions.
- Confirm the GPU is visible to the host and to a container.
- Note the exact driver branch, because this is the single most common source of a working setup silently breaking later.

## What I ran

_Not started._

## Output

_Pasted verbatim when the phase runs._

## What broke

_An empty section here after the phase is done would be suspicious, not impressive._

## What I would do differently

_Filled in after._

## Acceptance check

_The phase is done when this section holds pasted output, not when the commands have been typed._
